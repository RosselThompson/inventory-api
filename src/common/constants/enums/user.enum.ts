export enum UserRoleType {
  Admin = 'admin',
  Op = 'op',
}

export enum UserSortByType {
  CreatedAt = '_createdAt',
  Username = 'username',
  Role = 'role',
}

export enum UserSearchType {
  Username = 'username',
  Role = 'role',
  Nid = 'nid',
  Email = 'email',
}
